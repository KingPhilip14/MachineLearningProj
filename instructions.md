This file is temporary until the README is updated

# HOW TO START THE WEB APP:

1. In one terminal, change the directory to `/frontend/PAIGE`
2. In that directory, run the following command: `npm run dev`
3. In another terminal, run the following command to start the API: `uvicorn backend.api.main:app --reload`